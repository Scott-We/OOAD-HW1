Name: Scott Weygandt

I wrote my solution using Java in Intellj, draging the files into an empty standard Java project in Intellj should work, putting all the files in the same folder and compling them from the command line might work, but it might require import statements the IntellJ doesn't.